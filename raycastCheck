///
//ray - ray distance and lenght. start - starting position for raycast
vector ray = {0,0,0};
vector start = v@P + {0,0,0};
vector p;
float u,v;
if(intersect(1,start,ray,p, u, v) > -1)
    removepoint(0,@ptnum);
